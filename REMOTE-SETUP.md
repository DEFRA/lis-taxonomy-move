# Remote setup

Use this guide when the move index package is moved to its own hosted Git repository.

Suggested commands:

```bash
git remote add origin <repo-url>
git push -u origin main
git push origin --tags
```

Replace `<repo-url>` with the hosted repository URL for this project.
